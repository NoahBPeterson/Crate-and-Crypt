{
  "name": "crate-and-crypt-frontend",
  "version": "0.1.0",
  "module": "src/js/app.ts",
  "private": true,
  "scripts": {
    "dev": "bun run src/dev-server.ts",
    "start": "bun run --serve src/js/app.ts",
    "build": "bun build ./src/js/app.ts --outdir ./dist",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/three": "^0.162.0",
    "bun-types": "^1.0.30",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "three": "^0.162.0"
  }
}